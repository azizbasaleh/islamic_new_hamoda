import 'package:flutter/material.dart';

class MoshafScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Moshaf Screen"),
    );
  }
}
